"""
labs/dicom_lab/__init__.py

PLACEHOLDER – DICOM Processing Lab (not yet implemented).

Why this file exists:
  Reserves the `dicom_lab` package namespace so the import path
  `from labs.dicom_lab.routes import router` will work without changes
  once this lab is built.

Planned capabilities (future):
  - Parse and display DICOM metadata (patient info, modality, etc.)
  - Render DICOM pixel data with correct windowing/levelling
  - Export individual frames as PNG
  - Anonymise DICOM tags for research use
"""

LAB_NAME = "DICOM Processing Lab"
LAB_VERSION = "planned"
LAB_STATUS = "not_implemented"
